# Keyence SR Hardware Mount

## 设备需求

本模块通过 TCP/IP 与扫码器通信，不需要挂载 `/dev/ttyUSB*`。

- 扫码器 IP：`172.31.0.91`
- 扫码器端口：`9004`

## 网络要求

- 目标扫码器必须位于同一网络或可路由网络中。
- 容器使用 `network_mode: host`，以便访问宿主机网络。

## udev 示例

本模块无需 udev 规则；如部署涉及本地串口设备，不应在该模块中伪造映射。
