# ROS 2 工业视觉部署与开发操作手册

适用项目：Basler GigE / USB 相机、AprilTag 6D 位姿解算、WeChatQR 二维码识别与 Keyence SR 工业扫码器统一视觉系统。

适用环境：Ubuntu 22.04 LTS、ROS 2 Humble、Docker Engine 24.0+ 与 Docker Compose v2。

工作区路径：`/home/ubuntu/ros2_ws`

本文档面向现场运维人员、ROS 2 开发人员与产线集成工程师，覆盖环境配置、本地编译测试、Docker 生产镜像构建、容器编排部署、ROS 2 联调与维护排查。

---

## 1. 快速上手：生产最短启动路径

生产环境推荐使用统一的 `basler_camera` Docker 容器运行全部视觉链路。请勿在宿主机和容器内同时启动相机节点，避免硬件设备占用冲突。

在首次部署前，确认已在 [deploy/basler_camera/pylon-sdk/](deploy/basler_camera/pylon-sdk/) 目录下放置对应的 Basler pylon SDK 安装包，然后执行：

```bash
cd /home/ubuntu/ros2_ws

# 1. 准备现场配置文件
cp deploy/basler_camera/.env.example deploy/basler_camera/.env

# 2. 构建生产 Docker 镜像
DOCKER_BUILDKIT=1 docker build \
  -f deploy/basler_camera/Dockerfile \
  -t basler_camera_20260825_v1.0 .

# 3. 启动生产容器
docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  up -d

# 4. 检查容器状态与健康探针
docker inspect \
  --format '{{.State.Status}} {{.State.Health.Status}}' \
  basler_camera
```

启动初期状态通常为 `running starting`，等待约 10~30 秒后再次检查，最终状态应稳定为：

```text
running healthy
```

若状态异常，可查看实时容器日志：

```bash
docker logs -f basler_camera --tail 100
```

---

## 2. 运行模式与路径规范

### 2.1 生产模式 vs 本地开发模式

- **生产模式**：使用 [deploy/basler_camera/docker-compose.yml](deploy/basler_camera/docker-compose.yml) 与 [deploy/basler_camera/.env](deploy/basler_camera/.env) 启动容器，入口为 [src/industrial_vision_bringup/launch/vision_pipeline.launch.py](src/industrial_vision_bringup/launch/vision_pipeline.launch.py)。
- **本地开发模式**：在宿主机直接通过 `colcon build` 编译并在终端通过 `ros2 launch` 启动。调试前必须先停止生产容器（`docker stop basler_camera`）。

### 2.2 路径对应关系

| 用途 | 宿主机路径 | 容器内路径 |
|---|---|---|
| 工作区根目录 | `/home/ubuntu/ros2_ws` | `/opt/ros2_ws` |
| 源码目录 | `/home/ubuntu/ros2_ws/src` | `/opt/ros2_ws/src` |
| 相机 YAML 配置目录 | `/home/ubuntu/ros2_ws/deploy/basler_camera/config` | `/opt/ros2_ws/deploy/basler_camera/config` |
| ROS 2 安装空间 | `/opt/ros/humble` | `/opt/ros/humble` |
| 工作区 Install 空间 | `/home/ubuntu/ros2_ws/install` | `/opt/ros2_ws/install` |

> 注意：在 [deploy/basler_camera/.env](deploy/basler_camera/.env) 中配置路径时必须使用**容器内路径**，例如：
> `CAMERA_CONFIG_FILE=/opt/ros2_ws/deploy/basler_camera/config/aca2500_106611_18.yaml`

---

## 3. 环境与依赖检查

### 3.1 系统与 Docker 检查

```bash
lsb_release -a
docker --version
docker compose version
source /opt/ros/humble/setup.bash && ros2 --version
```

### 3.2 Basler pylon SDK 检查

Dockerfile 构建时通过 BuildKit bind mount 挂载 [deploy/basler_camera/pylon-sdk/](deploy/basler_camera/pylon-sdk/)。请确保该目录下存在且仅存在一份与系统架构匹配的 pylon 8.0+ SDK 安装包（`.tar.gz` 或 `.deb`）。

```bash
ls -lh /home/ubuntu/ros2_ws/deploy/basler_camera/pylon-sdk/
```

### 3.3 硬件网络连通性检查

```bash
# 测试 Basler 相机 IP
ping -c 3 172.31.0.88

# 测试 Keyence 扫码器 IP
ping -c 3 172.31.0.91
```

---

## 4. 配置文件详解

### 4.1 编辑 `.env` 文件

```bash
cp deploy/basler_camera/.env.example deploy/basler_camera/.env
```

常用参数说明：

| 变量名 | 默认值 | 说明与约束 |
|---|---|---|
| `BASLER_IMAGE` | `basler_camera_20260825_v1.0` | 容器镜像标签 |
| `BASLER_CONTAINER_NAME` | `basler_camera` | 容器名称 |
| `ROS_DOMAIN_ID` | `0` | ROS 2 DDS 域 ID |
| `RMW_IMPLEMENTATION` | `rmw_fastrtps_cpp` | DDS 实现中间件 |
| `CAMERA_ID` | `my_camera` | 相机流水线命名空间（必须为合法 ROS 标识符） |
| `CAMERA_CONFIG_FILE` | `/opt/ros2_ws/deploy/basler_camera/config/aca2500_106611_18.yaml` | 容器内相机参数配置文件路径 |
| `CAMERA_FRAME` | `basler_aca2500_106611_18` | 相机 TF 坐标系名称 |
| `CAMERA_MTU_SIZE` | `1500` | GigE 网卡 MTU 大小（范围：$576 \sim 9000$） |
| `CAMERA_RESPAWN` | `true` | 节点异常崩溃后是否自动按延迟重启 |
| `ENABLE_APRILTAG` | `true` | 是否加载 AprilTag 识别与位姿转发 |
| `ENABLE_QRCODE` | `false` | 是否加载 WeChatQR 深度学习识别与 6D 位姿解算 |
| `ENABLE_KEYENCE` | `true` | 是否加载 Keyence 工业扫码器驱动 |
| `SCANNER_IP` | `172.31.0.91` | Keyence 扫码器 IPv4 地址 |
| `SCANNER_PORT` | `9004` | Keyence TCP 端口（范围：$1 \sim 65535$） |
| `RECONNECT_INTERVAL_S` | `5.0` | Keyence 离线自动重连周期（秒，非负有限值） |
| `MEM_LIMIT` / `CPUS_LIMIT` | `4g` / `4.0` | 容器内存与 CPU 资源限制 |

### 4.2 双相机配置规则

若配置第二路相机流水线，必须提供 `CAMERA_ID_2`（例如 `cam2`）、`CAMERA_CONFIG_2` 与独立的 `CAMERA_FRAME_2`（例如 `basler_cam2`）。两路的参数、配置文件和坐标系名称不得冲突。

---

## 5. ROS 2 本地开发、编译与测试

### 5.1 工作区编译

```bash
cd /home/ubuntu/ros2_ws
source /opt/ros/humble/setup.bash

# 全量 Release 编译
colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

若仅需针对指定包编译：

```bash
colcon build --symlink-install \
  --packages-select pylon_ros2_camera_interfaces apriltag_pose_reader keyence_sr_wrapper qrcode_detector industrial_vision_bringup \
  --event-handlers console_direct+
source install/setup.bash
```

### 5.2 运行单元测试与集成测试

```bash
# 运行 pytest 单元测试套件
python3 -m pytest \
  src/keyence_sr_wrapper/test/ \
  src/apriltag_pose_reader/test/ \
  src/industrial_vision_bringup/test/ \
  src/qrcode_detector/test/test_qr_launch.py -v

# 运行 colcon test
colcon test --packages-select \
  pylon_ros2_camera_interfaces \
  apriltag_pose_reader \
  keyence_sr_wrapper \
  qrcode_detector \
  industrial_vision_bringup \
  --event-handlers console_cohesion+

colcon test-result --verbose
```

---

## 6. Docker 镜像构建与容器管理

### 6.1 构建生产镜像

```bash
cd /home/ubuntu/ros2_ws

DOCKER_BUILDKIT=1 docker build \
  -f deploy/basler_camera/Dockerfile \
  -t basler_camera_20260825_v1.0 .
```

### 6.2 容器生命周期操作

```bash
# 启动或强制重建容器
docker compose --env-file deploy/basler_camera/.env -f deploy/basler_camera/docker-compose.yml up -d --force-recreate

# 重启容器
docker compose --env-file deploy/basler_camera/.env -f deploy/basler_camera/docker-compose.yml restart

# 停止容器
docker compose --env-file deploy/basler_camera/.env -f deploy/basler_camera/docker-compose.yml down

# 查看已解析的 Compose 配置
docker compose --env-file deploy/basler_camera/.env -f deploy/basler_camera/docker-compose.yml config
```

---

## 7. ROS 2 运行验证与验收标准

### 7.1 节点拓扑检查

全功能启用时，每路流水线包含 6 个节点（3 个运行于组件容器内，3 个为独立进程）：

```bash
source /opt/ros/humble/setup.bash && source install/setup.bash
ros2 node list
```

期望输出包含：
- `/{camera_id}/pylon_ros2_camera_node`（组件）
- `/{camera_id}/apriltag`（组件，来自 `apriltag_ros`）
- `/{camera_id}/wechat_qr_node`（组件）
- `/{camera_id}/apriltag_pose_reader`（独立进程）
- `/{camera_id}/keyence_sr_node`（独立进程）
- `/{camera_id}/vision_status_aggregator`（独立进程）

### 7.2 话题与接口验证

| 验证项 | 验证命令 | 正常标准 |
|---|---|---|
| 相机出图 | `ros2 topic hz /{camera_id}/pylon_ros2_camera_node/image_raw` | 稳定在预期 FPS（如 5~20Hz） |
| 相机内参 | `ros2 topic echo /{camera_id}/pylon_ros2_camera_node/camera_info --once` | 包含有效标定参数（$f_x, f_y > 1.0$） |
| 二维码文本 | `ros2 topic echo /{camera_id}/qr/decoded_info --once` | 放入二维码后输出解码字符串 |
| 二维码位姿 | `ros2 topic echo /{camera_id}/qr/qr_pose --once` | 输出基于 `solvePnP` 解算的 `PoseStamped` |
| AprilTag 位姿 | `ros2 topic echo /{camera_id}/apriltag/pose --once` | 放入对应 ID 的 tag 后输出位姿 |
| AprilTag 变换 | `ros2 topic echo /{camera_id}/apriltag/transform --once` | 输出 `TransformStamped` 变换 |
| Keyence 扫码 | `ros2 service call /{camera_id}/scanner/trigger std_srvs/srv/Trigger {}` | 响应 `success=True` 并在 `/{camera_id}/scanner/barcode` 收到条码 |
| 流水线状态 | `ros2 topic echo /{camera_id}/vision/status --once` | `overall_level` 为 `0 (OK)` 或 `1 (WARN)` |

### 7.3 执行健康检查脚本

```bash
# 执行容器健康探针
docker exec basler_camera /opt/ros2_ws/deploy/basler_camera/healthcheck.sh

# 执行快速冒烟测试
docker exec basler_camera /opt/ros2_ws/deploy/basler_camera/smoke_test.sh
```

---

## 8. 机器人集成联调要点

1. **DDS 域隔离与一致性**：确保机器人控制器、宿主机和 Docker 容器使用相同的 `ROS_DOMAIN_ID` 与兼容的 `RMW_IMPLEMENTATION`（默认 `rmw_fastrtps_cpp`）。
2. **QoS 匹配**：相机图像与内参采用 `BEST_EFFORT` 策略与深度 1，订阅端请使用 `rclcpp::SensorDataQoS()` 或匹配的 Best Effort QoS 配置。
3. **坐标系统一与手眼标定**：使用 [scripts/handeye_calibrate.py](scripts/handeye_calibrate.py) 解算手眼标定参数后，可通过 Launch 参数 `handeye_calibration_file` 自动发布 `tool0 -> camera` 静态 TF 关系。

---

## 9. 常见问题排查速查

| 故障现象 | 可能原因 | 解决方向 |
|---|---|---|
| 容器不断重启或启动失败 | 配置文件语法错误、端口/MTU 非法、相机 IP 无法连接 | 查看 `docker logs basler_camera --tail 100` 定位首个异常 |
| 容器健康检查显示 `unhealthy` | 相机未发布 `camera_info` 或启用组件超过 5s 缺失诊断（`ERROR` 状态） | 检查相机连接、硬件供电与 `vision/status` 诊断消息 |
| 二维码未发布 `qr_pose` 话题 | 相机未标定（内参为占位符 $f_x, f_y \le 1.0$）或角点不完整 | 完成相机内参标定并加载真实标定 YAML |
| 宿主机无法发现容器内话题 | `ROS_DOMAIN_ID` 不一致或未采用 Host 网络 | 检查环境变量与 Compose 中的 `network_mode: host` |
| 修改 `.env` 或 YAML 未生效 | Docker 未重新加载配置 | 执行 `docker compose up -d --force-recreate` |


### 4.6 修改 YAML 或标定配置

相机运行参数位于：

```text
src/pylon_ros2_camera_wrapper/config/
deploy/basler_camera/config/
```

生产 Docker 镜像会复制 `deploy/basler_camera/config/`，Compose 运行时又会把宿主机的同名 `config` 目录以只读方式挂载到容器中。因此，生产现场修改配置后，优先修改：

```text
deploy/basler_camera/config/*.yaml
```

源码开发或手动 launch 使用的是：

```text
src/pylon_ros2_camera_wrapper/config/*.yaml
```

如果两套配置都要保持一致，应同步修改并记录变更。标定完成后，不要继续使用默认占位内参；确认 `camera_info` 中的 `fx`、`fy` 等值是真实标定结果。

---

## 5. 修改内容与后续操作对照

| 修改内容 | 后续操作 |
|---|---|
| 只修改 `.env` | 重新执行 `docker compose up -d`；必要时加 `--force-recreate` |
| 修改 `deploy/basler_camera/config/*.yaml` | 重新创建或重启容器，使节点重新读取配置 |
| 修改 `src` 中的 Python 代码 | 本地重新编译；生产环境重新构建镜像 |
| 修改 C++ 代码 | 本地重新编译；生产环境重新构建镜像 |
| 修改 launch 文件 | 本地重新编译；生产环境重新构建镜像 |
| 修改 Dockerfile | 重新构建镜像 |
| 修改 `docker-compose.yml` | 重新执行 Compose；必要时加 `--force-recreate` |
| 修改镜像 tag | 同步修改 `.env` 中的 `BASLER_IMAGE` |
| 修改模型文件 | 确认模型路径和文件完整，再重启或重建对应运行环境 |

配置文件通过 volume 挂载并不代表正在运行的节点会自动重新加载。修改后应重新创建容器，不能只观察宿主机文件时间变化。

---

## 6. ROS 2 本地开发、编译与测试

### 6.1 加载环境

每个新的终端都执行：

```bash
cd /home/ubuntu/ros2_ws
source /opt/ros/humble/setup.bash
```

工作区已经成功构建后，再执行：

```bash
source install/setup.bash
```

如果 `install/setup.bash` 不存在或 source 报错，说明工作区尚未成功构建，先执行编译。

### 6.2 全量 Release 编译

```bash
cd /home/ubuntu/ros2_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install \
  --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

说明：

- `colcon build` 编译工作区中的 ROS 2 包。
- `--symlink-install` 适合开发，Python 文件和资源修改后更容易生效。
- `Release` 模式适合现场运行。
- C++ 修改后必须重新编译。

### 6.3 按包编译

只修改某个功能时，可以缩小范围：

```bash
colcon build --symlink-install \
  --packages-select qrcode_detector \
  --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

常用包组合：

```bash
colcon build --symlink-install \
  --packages-select \
  pylon_ros2_camera_interfaces \
  apriltag_pose_reader \
  keyence_sr_wrapper \
  qrcode_detector \
  industrial_vision_bringup \
  --event-handlers console_direct+
source install/setup.bash
```

如果修改了接口定义，先编译 `pylon_ros2_camera_interfaces`，再编译依赖它的包。遇到依赖关系或安装空间异常时，使用全量编译确认问题。

### 6.4 运行 Python 单元测试

```bash
cd /home/ubuntu/ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
python3 -m pytest \
  src/keyence_sr_wrapper/test/ \
  src/apriltag_pose_reader/test/ \
  src/qrcode_detector/test/ \
  src/industrial_vision_bringup/test/ \
  -v
```

### 6.5 运行 colcon 测试

```bash
colcon test \
  --packages-select \
  pylon_ros2_camera_interfaces \
  apriltag_pose_reader \
  keyence_sr_wrapper \
  qrcode_detector \
  industrial_vision_bringup \
  --event-handlers console_cohesion+

colcon test-result --verbose
```

提交或部署前，应保存编译和测试结果。测试失败时先确认失败包、首个错误和是否为环境依赖问题，不要只看最后一行汇总。

### 6.6 编译后的最小检查

```bash
source /opt/ros/humble/setup.bash
source /home/ubuntu/ros2_ws/install/setup.bash

ros2 pkg prefix pylon_ros2_camera_interfaces
ros2 pkg prefix apriltag_pose_reader
ros2 pkg prefix qrcode_detector
ros2 pkg prefix industrial_vision_bringup
```

能返回安装路径，说明这些包已被当前 ROS 2 环境发现。

---

## 7. Docker 镜像构建

### 7.1 标准构建命令

必须在工作区根目录执行，因为 Dockerfile 需要访问 `src/`、`deploy/` 和 pylon SDK：

```bash
cd /home/ubuntu/ros2_ws

DOCKER_BUILDKIT=1 docker build \
  -f deploy/basler_camera/Dockerfile \
  -t basler_camera_20260825_v1.0 .
```

构建完成后检查镜像：

```bash
docker image ls basler_camera
```

### 7.2 使用 buildx 构建

构建速度较慢或需要明确启用 BuildKit 时：

```bash
docker buildx version

if ! docker buildx inspect ros2-builder >/dev/null 2>&1; then
  docker buildx create --name ros2-builder --driver docker-container --use
else
  docker buildx use ros2-builder
fi

docker buildx inspect --bootstrap
DOCKER_BUILDKIT=1 docker buildx build \
  --progress=plain \
  --load \
  -f deploy/basler_camera/Dockerfile \
  -t basler_camera_20260825_v1.0 .
```

`--load` 会把构建结果加载到本机 Docker 镜像列表，使 Compose 可以直接使用。

### 7.3 替换镜像源

Dockerfile 支持覆盖 Ubuntu 和 ROS 2 apt 源：

```bash
DOCKER_BUILDKIT=1 docker build \
  --build-arg UBUNTU_APT_MIRROR=http://mirrors.aliyun.com/ubuntu \
  --build-arg ROS2_APT_MIRROR=http://mirrors.tuna.tsinghua.edu.cn/ros2/ubuntu \
  -f deploy/basler_camera/Dockerfile \
  -t basler_camera_20260825_v1.0 .
```

基础镜像拉取失败时，可先准备等价的 ROS Humble 基础镜像并按现有 SOP 处理，不要随意更换 ROS 发行版或基础镜像标签。

### 7.4 构建缓存和磁盘空间

Dockerfile 已使用 apt 和 BuildKit 缓存。不要在正常构建前执行 `docker builder prune -f`，否则会删除缓存并重新下载大量依赖。

查看空间：

```bash
docker system df
df -h / /var/lib/docker 2>/dev/null || df -h
```

磁盘确实不足时，再谨慎清理：

```bash
docker builder prune -f
```

pylon SDK 通过 BuildKit bind mount 参与构建，不应被直接 `COPY` 到镜像层。若构建中出现 `No space left on device`，先检查是否错误地把数百 MB 的 SDK 压缩包复制进了镜像。

---

## 8. 启动、停止和重建容器

### 8.1 首次启动或配置更新

```bash
cd /home/ubuntu/ros2_ws
sudo -n docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  up -d
```

配置或 Compose 文件修改后，推荐明确重新创建：

```bash
sudo -n docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  up -d --force-recreate
```

### 8.2 镜像更新后的标准流程

```bash
cd /home/ubuntu/ros2_ws

DOCKER_BUILDKIT=1 docker build \
  -f deploy/basler_camera/Dockerfile \
  -t basler_camera_20260825_v1.0 .

sudo -n docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  up -d --force-recreate
```

### 8.3 日常启动、重启和停止

日常重启：

```bash
sudo -n docker restart basler_camera
```

停止但保留容器：

```bash
sudo -n docker stop basler_camera
```

启动已有容器：

```bash
sudo -n docker start basler_camera
```

停止并删除 Compose 容器：

```bash
sudo -n docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  down
```

`docker stop` 只停止容器，不删除容器和镜像。`docker compose down` 会删除 Compose 创建的容器，但不会自动删除镜像。

### 8.4 查看容器状态和配置

```bash
sudo -n docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  ps

sudo -n docker inspect \
  --format '{{.State.Status}} {{if .State.Health}}{{.State.Health.Status}}{{else}}no-healthcheck{{end}}' \
  basler_camera
```

查看 Compose 最终解析结果：

```bash
sudo -n docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  config > compose-rendered-config.yml
```

这个命令可以确认 `.env` 是否被正确加载，以及最终的镜像名、环境变量和挂载是否符合预期。输出文件如果包含现场设备信息，不要直接提交到公开仓库。

### 8.5 查看和保存日志

实时查看：

```bash
sudo -n docker logs -f --tail=200 basler_camera
```

保存日志：

```bash
sudo -n docker logs --tail=500 basler_camera \
  > basler_camera_$(date +%Y%m%d_%H%M%S).log
```

保存容器信息：

```bash
sudo -n docker inspect basler_camera \
  > basler_camera_inspect_$(date +%Y%m%d_%H%M%S).json
```

提交日志前检查并删除现场 IP、序列号或其他敏感信息。

---

## 9. ROS 2 运行验证与验收

### 9.1 容器内环境

在容器内执行 ROS 2 命令时，使用：

```bash
sudo -n docker exec basler_camera bash -lc \
  'source /opt/ros/humble/setup.bash && \
   source /opt/ros2_ws/install/setup.bash && \
   ros2 node list'
```

### 9.2 检查节点

宿主机和容器使用 `network_mode: host`，且 DDS 配置一致时，宿主机可以查看容器中的 ROS 2 节点：

```bash
source /opt/ros/humble/setup.bash
source /home/ubuntu/ros2_ws/install/setup.bash
ros2 node list
```

重点节点包括：

```text
/my_camera/pylon_ros2_camera_node
/my_camera/apriltag_pose_reader
/my_camera/wechat_qr_node
/my_camera/keyence_sr_node
```

被 `.env` 禁用的模块不应要求其节点存在。

### 9.3 检查相机图像和内参

```bash
ros2 topic type /my_camera/pylon_ros2_camera_node/image_raw
ros2 topic type /my_camera/pylon_ros2_camera_node/camera_info
ros2 topic info -v /my_camera/pylon_ros2_camera_node/image_raw
```

持续检查图像频率：

```bash
ros2 topic hz /my_camera/pylon_ros2_camera_node/image_raw
```

读取一条内参：

```bash
ros2 topic echo /my_camera/pylon_ros2_camera_node/camera_info --once
```

正常标准：

- `image_raw` 类型为 `sensor_msgs/msg/Image`。
- `camera_info` 类型为 `sensor_msgs/msg/CameraInfo`。
- 能够实际收到消息，而不仅是话题名称存在。
- 图像格式、分辨率和标定参数符合现场配置。

### 9.4 检查二维码

只有 `ENABLE_QRCODE=true` 时才执行：

```bash
ros2 topic echo /my_camera/qr/decoded_info --once
```

画面中没有二维码时没有结果，不一定表示节点故障。应先确认图像持续发布，再把二维码放入视野检查识别结果。

### 9.5 检查 AprilTag

将配置的 Tag 放入相机视野：

```bash
ros2 topic echo /my_camera/detections --once
ros2 topic echo /my_camera/apriltag/pose --once
ros2 topic echo /my_camera/apriltag/transform --once
```

识别结果会受到 Tag 规格、ID、距离、角度、曝光、内参和 TF 配置影响。图像正常并不保证一定有位姿结果。

### 9.6 检查 Keyence

先监听结果：

```bash
ros2 topic echo /my_camera/scanner/barcode
```

在另一个终端触发一次扫码：

```bash
ros2 service call /my_camera/scanner/trigger \
  std_srvs/srv/Trigger '{}'
```

Keyence 是典型的单次触发链路；二维码和 AprilTag 默认是持续处理图像的链路。

### 9.7 检查诊断和汇总状态

```bash
ros2 topic echo /my_camera/diagnostics --once
ros2 topic echo /my_camera/vision/status --once
```

健康检查会关注：

- 相机 `camera_info` 是否有正确类型并能收到消息。
- 启用的功能节点是否存在。
- `vision/status` 是否报告错误或过期状态。

启用模块的诊断状态为 `ERROR` 或 `STALE` 时，容器可能显示 `unhealthy`。

### 9.8 使用项目自带检查脚本

容器内冒烟测试：

```bash
sudo -n docker exec basler_camera \
  /opt/ros2_ws/deploy/basler_camera/smoke_test.sh
```

期望输出：

```text
SMOKE TEST PASSED: ... is publishing camera_info and image_raw
```

手动执行健康检查：

```bash
sudo -n docker exec basler_camera \
  /opt/ros2_ws/deploy/basler_camera/healthcheck.sh
```

期望输出：

```text
OK
```

---

## 10. 机器人程序联调注意事项

### 10.1 DDS 环境必须一致

宿主机、视觉容器和机器人程序至少应保持：

```text
ROS_DOMAIN_ID 相同
RMW_IMPLEMENTATION 相同或已确认兼容
```

检查当前终端：

```bash
echo "$ROS_DOMAIN_ID"
echo "$RMW_IMPLEMENTATION"
```

容器环境：

```bash
docker inspect basler_camera \
  --format '{{range .Config.Env}}{{println .}}{{end}}' \
  | grep -E 'ROS_DOMAIN_ID|RMW_IMPLEMENTATION'
```

### 10.2 主要接口

默认相机命名空间为 `/my_camera`。修改 `CAMERA_ID` 后，将前缀替换为对应的相机 ID。

| 功能 | 接口 |
|---|---|
| 原始图像 | `/<camera_id>/pylon_ros2_camera_node/image_raw` |
| 相机内参 | `/<camera_id>/pylon_ros2_camera_node/camera_info` |
| QR 结果 | `/<camera_id>/qr/decoded_info` |
| AprilTag 位姿 | `/<camera_id>/apriltag/pose` |
| AprilTag 变换 | `/<camera_id>/apriltag/transform` |
| Keyence 结果 | `/<camera_id>/scanner/barcode` |
| Keyence 触发 | `/<camera_id>/scanner/trigger` |
| 汇总状态 | `/<camera_id>/vision/status` |

不要只凭话题名称判断链路正常，应使用 `ros2 topic info -v`、`ros2 topic hz` 或 `ros2 topic echo --once` 确认消息实际流动。

### 10.3 查看图像

```bash
/home/ubuntu/ros2_ws/scripts/open_camera_rviz.sh
```

双相机启用 AprilTag 时，必须使用不同的 `CAMERA_FRAME` 和 `CAMERA_FRAME_2`，避免 TF frame 冲突。

---

## 11. 常见故障快速排查

| 现象 | 优先检查 | 处理方向 |
|---|---|---|
| 容器不断重启 | `docker logs basler_camera --tail=200` | 查看首个错误；重点检查相机、配置和参数格式 |
| 容器 `unhealthy` | healthcheck、节点列表、`vision/status` | 确认相机消息、启用模块和诊断状态 |
| 状态长时间 `starting` | 容器日志和相机网络 | 检查 `CAMERA_STARTUP_TIMEOUT_S`、相机是否连接或被占用 |
| 找不到相机 | 电源、网线、IP、pylon 设备身份 | 确认 YAML 的 `device_user_id` 与实际设备一致 |
| 相机节点存在但无 `image_raw` | `camera_info`、topic 类型和日志 | 检查相机连接、采集参数、设备占用 |
| 有图像但二维码无结果 | 图像内容、二维码距离角度、模块开关 | 确认 `ENABLE_QRCODE=true`、模型和 QR 后端配置 |
| 二维码节点不存在 | `.env` 中 `ENABLE_QRCODE` | 关闭是正常配置；启用后重新创建容器 |
| AprilTag 无位姿 | Tag family/ID、标定、TF | 先检查 `/detections`，再检查 pose 和 transform |
| Keyence 连接失败 | IP、端口、设备电源和网络 | 确认 `SCANNER_PORT` 合法且设备 TCP 服务可达 |
| 宿主机看不到容器话题 | `ROS_DOMAIN_ID`、DDS、网络模式 | 确认容器使用 host 网络且环境变量一致 |
| 修改配置不生效 | Compose 文件、`.env` 和容器创建时间 | 执行 `docker compose config`，再 `up -d --force-recreate` |
| Docker 构建空间不足 | `docker system df`、`df -h` | 检查 SDK 是否被错误 COPY，必要时清理构建缓存 |
| `install/setup.bash` 不存在 | 编译结果和工作区路径 | 回到工作区根目录，先完成 `colcon build` |

### 11.1 相机未发现或设备身份变化

查看容器日志：

```bash
sudo -n docker logs --tail=200 basler_camera
```

若出现设备身份不匹配，先枚举实际相机，再修改 YAML 中的设备身份。更换相机时也可能需要重新绑定设备 User ID：

```bash
/home/ubuntu/ros2_ws/install/pylon_ros2_camera_component/lib/pylon_ros2_camera_component/set_device_user_id \
  -sn <新相机序列号> <新的device_user_id>
```

修改后重新构建或重新启动相机链路，并确认 `CAMERA_CONFIG_FILE` 指向正确配置。

### 11.2 健康检查失败

先手动执行：

```bash
sudo -n docker exec basler_camera \
  /opt/ros2_ws/deploy/basler_camera/healthcheck.sh
```

再查看：

```bash
sudo -n docker inspect --format '{{json .State.Health}}' basler_camera
sudo -n docker logs --tail=300 basler_camera
```

不要通过关闭 healthcheck 来掩盖真实故障。健康检查是发现相机消息中断和启用模块异常的重要保护机制。

---

## 12. 镜像回滚

生产环境至少保留一个已经验收通过的旧镜像，不要覆盖或立即删除。

查看镜像：

```bash
docker image ls basler_camera
```

编辑 `.env`，切换到旧 tag：

```dotenv
BASLER_IMAGE=basler_camera_20260819_v2.0
```

重新创建容器：

```bash
sudo -n docker compose \
  --env-file deploy/basler_camera/.env \
  -f deploy/basler_camera/docker-compose.yml \
  up -d --force-recreate
```

确认状态：

```bash
sudo -n docker inspect \
  --format '{{.State.Status}} {{.State.Health.Status}}' \
  basler_camera
```

回滚后仍需执行冒烟测试和关键话题验证。

---

## 13. 发布前检查清单

### 环境

- [ ] Ubuntu 22.04 和 ROS 2 Humble 版本符合要求。
- [ ] Docker 和 Docker Compose v2 可用。
- [ ] pylon SDK 已按许可要求准备完成。
- [ ] 相机和 Keyence 网络、电源正常。
- [ ] 磁盘空间足够，旧版本镜像未被误删。

### 配置

- [ ] `.env` 已从模板创建并完成现场修改。
- [ ] `BASLER_IMAGE` 与实际镜像 tag 一致。
- [ ] `ROS_DOMAIN_ID` 与机器人程序一致。
- [ ] `CAMERA_ID`、设备身份和配置文件匹配。
- [ ] Keyence IP 和端口正确。
- [ ] 端口、超时和重连参数格式合法。
- [ ] 双相机 ID、配置文件和 TF frame 均不重复。
- [ ] 标定参数是真实标定结果，而不是占位值。

### 构建与测试

- [ ] 本地 `colcon build` 成功，或 Docker 镜像构建成功。
- [ ] Python 单元测试通过。
- [ ] `colcon test-result --verbose` 无阻断性失败。
- [ ] 镜像 tag 使用日期和版本号，旧版本仍可回滚。

### 运行验收

- [ ] 容器最终状态为 `running healthy`。
- [ ] `smoke_test.sh` 输出 `SMOKE TEST PASSED`。
- [ ] `camera_info` 和 `image_raw` 能收到实际消息。
- [ ] 已启用模块的节点存在。
- [ ] 已启用的 QR、AprilTag、Keyence 链路完成实际验证。
- [ ] 机器人程序能看到需要的 ROS 2 话题和服务。
- [ ] 未在宿主机重复启动相机节点。

---

## 14. 生产环境重要注意事项

1. 生产环境统一使用 `basler_camera` 容器，不要混用已经移除或过时的独立容器脚本。
2. 不要让宿主机手动 launch 和 Docker 容器同时连接同一台相机。
3. 不要把宿主机路径写入容器内的 `CAMERA_CONFIG_FILE`。
4. 不要只执行 `docker restart` 来应用新的 Compose 环境变量；配置变化后使用 `docker compose up -d --force-recreate`。
5. 不要随意修改 ROS 2 话题、服务、TF frame 或 camera ID；修改时必须同步机器人侧程序。
6. 不要删除唯一可回滚的旧镜像。
7. 不要随意升级 OpenCV、NumPy 或替换 pip OpenCV 包；当前 QR 与 `cv_bridge` 依赖有明确版本约束。
8. 不要把真实 pylon SDK、现场 `.env`、设备日志和包含现场信息的 Compose 展开文件提交到不受控的位置。
9. 相机、二维码和 AprilTag 的检测结果依赖实际视野、光照、曝光、距离、角度和标定，不能只用“节点存在”作为业务验收标准。

---

## 15. 相关文档

- [项目总说明](README.md)
- [快速启动指南](项目启动运行指南/快速启动指南.md)
- [工控机集成与运维指南](项目启动运行指南/工控机集成与运维指南.md)
- [Basler 相机构建部署与故障处理 SOP](项目启动运行指南/Basler相机构建部署与故障处理SOP.md)
- [交接保留运行要点](交接保留运行要点.md)
- [ROS 2 运行验证与验收](#9-ros-2-运行验证与验收)
- [Docker 部署说明](deploy/basler_camera/README.md)
- [Basler ROS 2 接口](deploy/basler_camera/ROS2-INTERFACE.md)
